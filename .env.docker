# Docker 环境配置示例
# 复制此文件为 .env 并修改相应值

# 服务器配置
PORT=3000
NODE_ENV=production

# 数据库配置
DB_NAME=job_tracker
DB_USER=lthero
DB_PASSWORD=Www_159159
DB_HOST=db
DB_PORT=5432

# JWT 配置（生产环境必须修改为复杂的随机字符串！）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# 前端地址（用于 CORS）
FRONTEND_URL=http://localhost:3000
